<Window x:Class="Prism.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:Prism.ViewModels"
        mc:Ignorable="d"
        Title="系统登录" Height="550" Width="420" 
        WindowStartupLocation="CenterScreen" 
        Background="#F4F7F9" ResizeMode="NoResize"
        FontFamily="Microsoft YaHei">
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>
    <Grid>
        <Border Background="White" CornerRadius="20" Width="340" 
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" Color="#C0C0C0" ShadowDepth="4" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel Margin="35,40">
                <TextBlock Text="Login" FontSize="28" FontWeight="ExtraBold" 
                           Foreground="#007AFF" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="请输入您的凭据以访问系统" FontSize="13" 
                           Foreground="#999" HorizontalAlignment="Center" Margin="0,0,0,35"/>

                <TextBlock Text="用户名" FontSize="12" FontWeight="Bold" Foreground="#666" Margin="0,0,0,8"/>
                <TextBox Style="{StaticResource LoginInputStyle}"
                         Text="{Binding LoginModel.UserName, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="密码" FontSize="12" FontWeight="Bold" Foreground="#666" Margin="0,0,0,8"/>
                <TextBox Style="{StaticResource LoginInputStyle}"
                         Text="{Binding LoginModel.UserPsw, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="忘记密码？" 
        Style="{StaticResource LinkButtonStyle}"
        Command="{Binding ShowResetPswCommand}"
        HorizontalAlignment="Right"
        FontSize="11"
        Margin="0,5,0,0"/>
                <Button Content="登 录" 
                        Command="{Binding LoginCommand}"
                        IsDefault="True"
                        Background="#007AFF" Foreground="White" FontSize="16" FontWeight="Bold" 
                        Height="45" Margin="0,10,0,0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
                    <TextBlock Text="还没有账号？" Foreground="#888" VerticalAlignment="Center"/>
                    <Button Content="立即注册" 
                            Style="{StaticResource LinkButtonStyle}"
                            Command="{Binding RegisterToShowCommand}"
                            Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>