<Window x:Class="Prism.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:vm="clr-namespace:Prism.ViewModels"
        mc:Ignorable="d"
        Title="用户注册" Height="600" Width="400" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" >

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Border Margin="10" Background="White" CornerRadius="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="0" ShadowDepth="0" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <StackPanel Margin="40,20">
                <TextBlock Text="创建您的账号" FontSize="26" FontWeight="Bold" Margin="0,10,0,5" HorizontalAlignment="Center" Foreground="#333"/>
                <TextBlock Text="请填写以下信息完成注册" FontSize="12" Foreground="Gray" Margin="0,0,0,25" HorizontalAlignment="Center"/>

                <TextBlock Text="用户名" FontSize="12" Foreground="#555" Margin="0,5,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,0,0,15">
                    <TextBox Text="{Binding LoginModel.UserName, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="30" VerticalContentAlignment="Center" FontSize="14"/>
                </Border>

                <TextBlock Text="注册邮箱" FontSize="12" Foreground="#555" Margin="0,5,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,0,0,15">
                    <TextBox Text="{Binding LoginModel.Email, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="30" VerticalContentAlignment="Center" FontSize="14" ToolTip="用于找回密码"/>
                </Border>

                <TextBlock Text="设置密码" FontSize="12" Foreground="#555" Margin="0,5,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,0,0,15" >
                    <TextBox Text="{Binding LoginModel.UserPsw, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="30" VerticalContentAlignment="Center"/>
                </Border>

                <TextBlock Text="确认密码" FontSize="12" Foreground="#555" Margin="0,5,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,0,0,25">
                    <TextBox Text="{Binding LoginModel.UserConfirmPsw, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" Height="30" VerticalContentAlignment="Center"/>
                </Border>

                <Button Content="立即注册" 
                        Command="{Binding RegisterCommand}"
                        Height="45" Background="#1A73E8" Foreground="White" FontSize="16" FontWeight="SemiBold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1557B0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="已有账号？返回登录" 
                        Command="{Binding BackToLoginCommand}"
                        Background="Transparent" BorderThickness="0" Foreground="#1A73E8" Margin="0,20,0,0" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>